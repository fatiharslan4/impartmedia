// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package dbmodels

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// UserDemographic is an object representing the database table.
type UserDemographic struct {
	DemographicID uint64 `boil:"demographic_id" json:"demographic_id" toml:"demographic_id" yaml:"demographic_id"`
	AnswerID      uint   `boil:"answer_id" json:"answer_id" toml:"answer_id" yaml:"answer_id"`
	UserCount     int    `boil:"user_count" json:"user_count" toml:"user_count" yaml:"user_count"`

	R *userDemographicR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userDemographicL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserDemographicColumns = struct {
	DemographicID string
	AnswerID      string
	UserCount     string
}{
	DemographicID: "demographic_id",
	AnswerID:      "answer_id",
	UserCount:     "user_count",
}

var UserDemographicTableColumns = struct {
	DemographicID string
	AnswerID      string
	UserCount     string
}{
	DemographicID: "user_demographic.demographic_id",
	AnswerID:      "user_demographic.answer_id",
	UserCount:     "user_demographic.user_count",
}

// Generated where

var UserDemographicWhere = struct {
	DemographicID whereHelperuint64
	AnswerID      whereHelperuint
	UserCount     whereHelperint
}{
	DemographicID: whereHelperuint64{field: "`user_demographic`.`demographic_id`"},
	AnswerID:      whereHelperuint{field: "`user_demographic`.`answer_id`"},
	UserCount:     whereHelperint{field: "`user_demographic`.`user_count`"},
}

// UserDemographicRels is where relationship names are stored.
var UserDemographicRels = struct {
	Answer string
}{
	Answer: "Answer",
}

// userDemographicR is where relationships are stored.
type userDemographicR struct {
	Answer *Answer `boil:"Answer" json:"Answer" toml:"Answer" yaml:"Answer"`
}

// NewStruct creates a new relationship struct
func (*userDemographicR) NewStruct() *userDemographicR {
	return &userDemographicR{}
}

// userDemographicL is where Load methods for each relationship are stored.
type userDemographicL struct{}

var (
	userDemographicAllColumns            = []string{"demographic_id", "answer_id", "user_count"}
	userDemographicColumnsWithoutDefault = []string{"answer_id"}
	userDemographicColumnsWithDefault    = []string{"demographic_id", "user_count"}
	userDemographicPrimaryKeyColumns     = []string{"demographic_id"}
)

type (
	// UserDemographicSlice is an alias for a slice of pointers to UserDemographic.
	// This should almost always be used instead of []UserDemographic.
	UserDemographicSlice []*UserDemographic
	// UserDemographicHook is the signature for custom UserDemographic hook methods
	UserDemographicHook func(context.Context, boil.ContextExecutor, *UserDemographic) error

	userDemographicQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userDemographicType                 = reflect.TypeOf(&UserDemographic{})
	userDemographicMapping              = queries.MakeStructMapping(userDemographicType)
	userDemographicPrimaryKeyMapping, _ = queries.BindMapping(userDemographicType, userDemographicMapping, userDemographicPrimaryKeyColumns)
	userDemographicInsertCacheMut       sync.RWMutex
	userDemographicInsertCache          = make(map[string]insertCache)
	userDemographicUpdateCacheMut       sync.RWMutex
	userDemographicUpdateCache          = make(map[string]updateCache)
	userDemographicUpsertCacheMut       sync.RWMutex
	userDemographicUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var userDemographicBeforeInsertHooks []UserDemographicHook
var userDemographicBeforeUpdateHooks []UserDemographicHook
var userDemographicBeforeDeleteHooks []UserDemographicHook
var userDemographicBeforeUpsertHooks []UserDemographicHook

var userDemographicAfterInsertHooks []UserDemographicHook
var userDemographicAfterSelectHooks []UserDemographicHook
var userDemographicAfterUpdateHooks []UserDemographicHook
var userDemographicAfterDeleteHooks []UserDemographicHook
var userDemographicAfterUpsertHooks []UserDemographicHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UserDemographic) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDemographicBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UserDemographic) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDemographicBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UserDemographic) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDemographicBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UserDemographic) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDemographicBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UserDemographic) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDemographicAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UserDemographic) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDemographicAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UserDemographic) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDemographicAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UserDemographic) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDemographicAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UserDemographic) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userDemographicAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUserDemographicHook registers your hook function for all future operations.
func AddUserDemographicHook(hookPoint boil.HookPoint, userDemographicHook UserDemographicHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		userDemographicBeforeInsertHooks = append(userDemographicBeforeInsertHooks, userDemographicHook)
	case boil.BeforeUpdateHook:
		userDemographicBeforeUpdateHooks = append(userDemographicBeforeUpdateHooks, userDemographicHook)
	case boil.BeforeDeleteHook:
		userDemographicBeforeDeleteHooks = append(userDemographicBeforeDeleteHooks, userDemographicHook)
	case boil.BeforeUpsertHook:
		userDemographicBeforeUpsertHooks = append(userDemographicBeforeUpsertHooks, userDemographicHook)
	case boil.AfterInsertHook:
		userDemographicAfterInsertHooks = append(userDemographicAfterInsertHooks, userDemographicHook)
	case boil.AfterSelectHook:
		userDemographicAfterSelectHooks = append(userDemographicAfterSelectHooks, userDemographicHook)
	case boil.AfterUpdateHook:
		userDemographicAfterUpdateHooks = append(userDemographicAfterUpdateHooks, userDemographicHook)
	case boil.AfterDeleteHook:
		userDemographicAfterDeleteHooks = append(userDemographicAfterDeleteHooks, userDemographicHook)
	case boil.AfterUpsertHook:
		userDemographicAfterUpsertHooks = append(userDemographicAfterUpsertHooks, userDemographicHook)
	}
}

// One returns a single userDemographic record from the query.
func (q userDemographicQuery) One(ctx context.Context, exec boil.ContextExecutor) (*UserDemographic, error) {
	o := &UserDemographic{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: failed to execute a one query for user_demographic")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all UserDemographic records from the query.
func (q userDemographicQuery) All(ctx context.Context, exec boil.ContextExecutor) (UserDemographicSlice, error) {
	var o []*UserDemographic

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "dbmodels: failed to assign all query results to UserDemographic slice")
	}

	if len(userDemographicAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all UserDemographic records in the query.
func (q userDemographicQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to count user_demographic rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q userDemographicQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: failed to check if user_demographic exists")
	}

	return count > 0, nil
}

// Answer pointed to by the foreign key.
func (o *UserDemographic) Answer(mods ...qm.QueryMod) answerQuery {
	queryMods := []qm.QueryMod{
		qm.Where("`answer_id` = ?", o.AnswerID),
	}

	queryMods = append(queryMods, mods...)

	query := Answers(queryMods...)
	queries.SetFrom(query.Query, "`answer`")

	return query
}

// LoadAnswer allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userDemographicL) LoadAnswer(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUserDemographic interface{}, mods queries.Applicator) error {
	var slice []*UserDemographic
	var object *UserDemographic

	if singular {
		object = maybeUserDemographic.(*UserDemographic)
	} else {
		slice = *maybeUserDemographic.(*[]*UserDemographic)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &userDemographicR{}
		}
		args = append(args, object.AnswerID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userDemographicR{}
			}

			for _, a := range args {
				if a == obj.AnswerID {
					continue Outer
				}
			}

			args = append(args, obj.AnswerID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`answer`),
		qm.WhereIn(`answer.answer_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Answer")
	}

	var resultSlice []*Answer
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Answer")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for answer")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for answer")
	}

	if len(userDemographicAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Answer = foreign
		if foreign.R == nil {
			foreign.R = &answerR{}
		}
		foreign.R.UserDemographics = append(foreign.R.UserDemographics, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AnswerID == foreign.AnswerID {
				local.R.Answer = foreign
				if foreign.R == nil {
					foreign.R = &answerR{}
				}
				foreign.R.UserDemographics = append(foreign.R.UserDemographics, local)
				break
			}
		}
	}

	return nil
}

// SetAnswer of the userDemographic to the related item.
// Sets o.R.Answer to related.
// Adds o to related.R.UserDemographics.
func (o *UserDemographic) SetAnswer(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Answer) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE `user_demographic` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, []string{"answer_id"}),
		strmangle.WhereClause("`", "`", 0, userDemographicPrimaryKeyColumns),
	)
	values := []interface{}{related.AnswerID, o.DemographicID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AnswerID = related.AnswerID
	if o.R == nil {
		o.R = &userDemographicR{
			Answer: related,
		}
	} else {
		o.R.Answer = related
	}

	if related.R == nil {
		related.R = &answerR{
			UserDemographics: UserDemographicSlice{o},
		}
	} else {
		related.R.UserDemographics = append(related.R.UserDemographics, o)
	}

	return nil
}

// UserDemographics retrieves all the records using an executor.
func UserDemographics(mods ...qm.QueryMod) userDemographicQuery {
	mods = append(mods, qm.From("`user_demographic`"))
	return userDemographicQuery{NewQuery(mods...)}
}

// FindUserDemographic retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUserDemographic(ctx context.Context, exec boil.ContextExecutor, demographicID uint64, selectCols ...string) (*UserDemographic, error) {
	userDemographicObj := &UserDemographic{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `user_demographic` where `demographic_id`=?", sel,
	)

	q := queries.Raw(query, demographicID)

	err := q.Bind(ctx, exec, userDemographicObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "dbmodels: unable to select from user_demographic")
	}

	if err = userDemographicObj.doAfterSelectHooks(ctx, exec); err != nil {
		return userDemographicObj, err
	}

	return userDemographicObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UserDemographic) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no user_demographic provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userDemographicColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userDemographicInsertCacheMut.RLock()
	cache, cached := userDemographicInsertCache[key]
	userDemographicInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userDemographicAllColumns,
			userDemographicColumnsWithDefault,
			userDemographicColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(userDemographicType, userDemographicMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userDemographicType, userDemographicMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `user_demographic` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `user_demographic` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `user_demographic` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, userDemographicPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to insert into user_demographic")
	}

	var lastID int64
	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.DemographicID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == userDemographicMapping["demographic_id"] {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.DemographicID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to populate default values for user_demographic")
	}

CacheNoHooks:
	if !cached {
		userDemographicInsertCacheMut.Lock()
		userDemographicInsertCache[key] = cache
		userDemographicInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the UserDemographic.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UserDemographic) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	userDemographicUpdateCacheMut.RLock()
	cache, cached := userDemographicUpdateCache[key]
	userDemographicUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userDemographicAllColumns,
			userDemographicPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("dbmodels: unable to update user_demographic, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `user_demographic` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, userDemographicPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userDemographicType, userDemographicMapping, append(wl, userDemographicPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update user_demographic row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by update for user_demographic")
	}

	if !cached {
		userDemographicUpdateCacheMut.Lock()
		userDemographicUpdateCache[key] = cache
		userDemographicUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q userDemographicQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all for user_demographic")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected for user_demographic")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserDemographicSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("dbmodels: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userDemographicPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `user_demographic` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, userDemographicPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to update all in userDemographic slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to retrieve rows affected all in update all userDemographic")
	}
	return rowsAff, nil
}

var mySQLUserDemographicUniqueColumns = []string{
	"demographic_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UserDemographic) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("dbmodels: no user_demographic provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userDemographicColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLUserDemographicUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userDemographicUpsertCacheMut.RLock()
	cache, cached := userDemographicUpsertCache[key]
	userDemographicUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			userDemographicAllColumns,
			userDemographicColumnsWithDefault,
			userDemographicColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			userDemographicAllColumns,
			userDemographicPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("dbmodels: unable to upsert user_demographic, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`user_demographic`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `user_demographic` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(userDemographicType, userDemographicMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userDemographicType, userDemographicMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	result, err := exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to upsert for user_demographic")
	}

	var lastID int64
	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	lastID, err = result.LastInsertId()
	if err != nil {
		return ErrSyncFail
	}

	o.DemographicID = uint64(lastID)
	if lastID != 0 && len(cache.retMapping) == 1 && cache.retMapping[0] == userDemographicMapping["demographic_id"] {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(userDemographicType, userDemographicMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to retrieve unique values for user_demographic")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to populate default values for user_demographic")
	}

CacheNoHooks:
	if !cached {
		userDemographicUpsertCacheMut.Lock()
		userDemographicUpsertCache[key] = cache
		userDemographicUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single UserDemographic record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UserDemographic) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("dbmodels: no UserDemographic provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userDemographicPrimaryKeyMapping)
	sql := "DELETE FROM `user_demographic` WHERE `demographic_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete from user_demographic")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by delete for user_demographic")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q userDemographicQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("dbmodels: no userDemographicQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from user_demographic")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for user_demographic")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserDemographicSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(userDemographicBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userDemographicPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `user_demographic` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, userDemographicPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: unable to delete all from userDemographic slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "dbmodels: failed to get rows affected by deleteall for user_demographic")
	}

	if len(userDemographicAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UserDemographic) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUserDemographic(ctx, exec, o.DemographicID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserDemographicSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserDemographicSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userDemographicPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `user_demographic`.* FROM `user_demographic` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, userDemographicPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "dbmodels: unable to reload all in UserDemographicSlice")
	}

	*o = slice

	return nil
}

// UserDemographicExists checks if the UserDemographic row exists.
func UserDemographicExists(ctx context.Context, exec boil.ContextExecutor, demographicID uint64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `user_demographic` where `demographic_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, demographicID)
	}
	row := exec.QueryRowContext(ctx, sql, demographicID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "dbmodels: unable to check if user_demographic exists")
	}

	return exists, nil
}
